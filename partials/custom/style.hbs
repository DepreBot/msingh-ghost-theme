
        .home-template #mc_embed_signup {
            -moz-box-shadow: 0px 2px 7px rgba(0,0,0,0.25);
            -webkit-box-shadow: 0px 2px 7px rgba(0,0,0,0.25);
            box-shadow: 0px 2px 7px rgba(0,0,0,0.25);
            border-radius: 4px;
        }
        .drawer-button{ top:60px; }

        #mc_embed_signup {
           text-align: center;
           padding: 20px 10px;
        }
        #mc_embed_signup input[type=text],
        #mc_embed_signup input[type=email] {
            display: inline-block;
            position: relative;
            height: 40px;
            padding: 0 20px;
            border: 0;
            font-size: 14px;
            line-height: 39px;
            text-decoration: none;
            font-style: normal;
            vertical-align: bottom;
            font-weight: 400;
            margin: 0px !important;
            border-radius: 3px;
        }

        .btn-mini {
                height:28px !important;
                line-height:28px !important;
                margin-bottom: 4px;
                background-color: #1abc9c !important;
                color: #fff !important;
                /*font-size: 12px !important;*/
        }
        .tweet-me { display: inline-table; margin-top: 5px; margin-bottom: -5px; }

        .cover-container { color: #fff !important; text-shadow: 10px 10px 10px #000, -10px -10px 10px #000, !important; }
        .cover-description a{ color: #fff; }

        .wrapper-container{max-width:980px;}
        .post-footer>.post-social{width:30%;}
        .post-footer>.post-author{width:70%;}

        .drawer-container { padding-top: 60px; }

        footer p { color:#666; }
        footer a { color:#666 !important; text-decoration:none; }
        footer .links{ font-size: 14px; margin-bottom: 20px; }
        footer .links .seprator{ margin: 0 5px; }

        @media only screen and (max-width: 767px) {
            #hellobar_container, #hellobar_pusher { display: none; }
            #drawer-button { top: 10px; }
            .drawer-container { padding-top: 10px; }
        }

