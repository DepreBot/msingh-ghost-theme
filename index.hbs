{{!< default}}

<aside role="banner" class="cover">
  <div {{#if @blog.cover}}data-load-image="{{@blog.cover}}"{{/if}} class="cover-image"></div>
  <div class="cover-container">
    {{#if @blog.logo}}
      <a href="{{@blog.url}}" class="cover-logo" data-pjax>
        <img src="{{@blog.logo}}" alt="{{@blog.title}}">
      </a>
    {{/if}}
    <h1 class="cover-title">{{@blog.title}}</h1>
    <p class="cover-description">Founder of <a href="http://www.websterfolks.com">WebsterFolks</a>, Working on <a href="http://www.microworkr.com">MicroWorkr</a> (Coming Soon), Love Food, Web Developer & Entrepreneur From India.</p>
  </div>
</aside>

<section class="wrapper">
  <div class="wrapper-container">
    {{> custom/featured this}}

    <section class="post-list">
      {{#foreach posts}}
        {{> post/list this}}
      {{/foreach}}
    </section>

    {{#if pagination}}
      {{pagination}}
    {{/if}}

    {{> footer this}}
  </div>
</section>
